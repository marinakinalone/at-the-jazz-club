
    // animation then modal + button to:
        // unlock scene
    // update interactive areas
    import React from 'react'
    
    const WinningMemoryGame = () => {
      return (
        <div>WinningMessage for Memory Game (two versions _one for first unlock, the other one for replay)</div>
      )
    }
    
    export default WinningMemoryGame